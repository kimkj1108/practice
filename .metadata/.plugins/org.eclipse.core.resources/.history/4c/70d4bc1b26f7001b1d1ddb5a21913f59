package singleton;
//메모리영역(Heap)에 객체를 한 번만 생성.
//싱글톤은 디자인 패턴의 하나.
public class singleton {
	//1. 변수를 담아줄 객체를 선언
	//2. 이때 외부에서 생성하지 못하도록, private으로 생성한다.
	//접근제한자 private은 같은 클래스에서만 접근이 가능하다.
	
	private static 

}
